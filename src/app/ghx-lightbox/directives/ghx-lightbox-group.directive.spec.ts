import { GhxLightboxGroupDirective } from './ghx-lightbox-group.directive';

describe('GhxLightboxGroupDirective', () => {
  it('should create an instance', () => {
    const directive = new GhxLightboxGroupDirective();
    expect(directive).toBeTruthy();
  });
});
